version: '3.8'

services:
  app:
    build: .
    ports:
      - $PORT:$PORT
    env_file:
      - src/.env
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
